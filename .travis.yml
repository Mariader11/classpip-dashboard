sudo: false
language: node_js
node_js:
  - '6'
before_script:
  - npm install -g angular-cli
script:
  - ng lint
before_deploy:
  - rm -rf dist node_modules tmp typings
deploy:
  provider: script
  script: scripts/deploy.sh
  on:
    branch: master

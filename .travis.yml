sudo: required
language: node_js
node_js: 8

services:
  - docker

script:
  - npm run lint
  - npm run test
  - npm run coveralls
  - docker build -t classpip/classpip-dashboard .

deploy:
  provider: script
  script: ./deploy.sh
  on:
    branch: master
